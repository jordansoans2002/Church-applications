<UserControl x:Class="Create_PPT_UI.View.Settings"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Create_PPT_UI.View"
             mc:Ignorable="d" 
             d:DesignHeight="25" d:DesignWidth="200">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="2" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <TextBlock Text="{Binding settingName}" VerticalAlignment="Center"/>
        <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" />
        <TextBox Grid.Column="2" Text="{Binding settingValue}"
                 VerticalContentAlignment="Center"
                 BorderBrush="Transparent" Padding="0,2,0,2"
                 />
    </Grid>
</UserControl>
